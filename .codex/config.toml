projects = { "/home/devuser/workspace" = { trust_level = "trusted" } }
model = "gpt-5.4"
model_provider = "openai"
approval_policy = "never"
# sandbox_mode = "workspace-write"
# この template の shipped default は、承認待ちを挟まない代わりに
# danger-full-access を採用する reviewed default。
# generated repo は bootstrap 時に .codex/config.toml を見直し、
# threat model に応じて workspace-write に tighten するか、
# 必要なら他設定も明示的に調整する。
# danger-full-access を使う generated repo は、少なくとも mount 範囲、
# 秘密情報、外向き通信、container の破棄容易性を見直す。
# rationale と見直し条件は docs/architecture/decision-records/
# codex-shared-defaults.md と
# 2026-03-30-codex-workspace-write-bwrap-sandbox.md を参照。
# approval_policy = "on-request"
sandbox_mode = "danger-full-access"

model_reasoning_effort = "xhigh"
plan_mode_reasoning_effort = "xhigh"
personality = "pragmatic"


[sandbox_workspace_write]
network_access = false

[profiles.very-fast]
model_reasoning_effort = "minimal"

[profiles.fast]
model_reasoning_effort = "low"

[profiles.base]
model_reasoning_effort = "medium"

[profiles.deep]
model_reasoning_effort = "high"

[sandbox_read_only]
network_access = false
